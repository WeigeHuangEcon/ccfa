devtools::install_github("bcallaway11/TempleMetrics")

lessthan <- function(x,y) {
    1*(x <= y)
}

cfa <- function(yvals, tvals, yname, tname, xnames, data) {
    pb <- progress::progress_bar$new(total=length(tvals))
    y <- data[,yname]
    indicatormatrix <- outer(y, yvals, "lessthan")
    vapply(yvals, 
    
